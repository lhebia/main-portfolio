import React, { ReactElement } from 'react';
import "./styles.scss";

const Card: React.FC<any> = ({ post }): ReactElement => {
  return (
    <div>
      <p>Something</p>
    </div>
  )
}

export default Card;
